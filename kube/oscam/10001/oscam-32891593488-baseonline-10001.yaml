apiVersion: apps/v1
kind: Deployment
metadata:
  name: oscam-32891593488-baseonline-10001
  labels:
    app: oscam-server
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: oscam-10001
  template:
    metadata:
      labels:
        app: oscam-10001
    spec:
      hostNetwork: true
      containers:
      - image: localhost:5000/oscam:v1
        name: oscam-32891593488-baseonline-10001
        tty: yes
        securityContext:
          privileged: true
        volumeMounts:
        - name: "10001"
          mountPath: /etc/oscam
        - name: tz-moscow
          mountPath: /etc/localtime
      volumes:
        - name: "10001"
          configMap:
            name: "10001"
        - name: tz-moscow
          hostPath:
            path: /usr/share/zoneinfo/Europe/Moscow

